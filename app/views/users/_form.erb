


<%= form_for @user do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :field %>
  <%= f.select(:borough, options_for_select(["New York", "Bronx", "Staten Island", "Queens", "Brooklyn"]), prompt: true) %>
  <%= f.submit %>
<% end %>
